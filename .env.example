OPENAI_API_KEY=your_openai_api_key_here
# Optional: override default model (see OpenAI Agents SDK / your account)
# OPENAI_AGENT_MODEL=gpt-4o-mini
# Optional: where to store conversation history (default: history.json in cwd)
# HISTORY_JSON_PATH=history.json
